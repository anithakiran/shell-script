The line keyword in a shell script is used to


 display the current line number off the script. 
 It is a special variable that is automatically set by the shell when the script is executed. 
 
 The value off the line keyword is an integer that represents the line number off the current line off the script.



 The line keyword in a shell script is used to display the current line number off the script.
 
  It is a special variable that is automatically set by the shell when the script is executed. 
  
  The value off the line keyword is an integer that represents the line number off the current line off the script.